flask>=3.0.0
flask-cors>=4.0.0
dash>=2.17.0
dash-bootstrap-components>=1.6.0
requests>=2.31.0
bcrypt>=4.0.1
gunicorn>=21.2.0
metapub>=0.6.4
habanero>=1.2.6
unpywall>=0.2.0
semanticscholar>=0.8.0
sentence-transformers>=2.2.2
arxiv>=2.0.0
PyMuPDF>=1.23.0
watchdog>=3.0.0

# Optional: Web of Science Expanded API integration
# Requires WOS_API_KEY environment variable (no additional package needed)
# Get API key from https://developer.clarivate.com/
# Example: export WOS_API_KEY="your-key-here"

# Optional: Literature Review (ASReview Integration)
# ASReview service runs on separate GPU-enabled host
# No additional Python packages needed for HARVEST (uses requests for API calls)
# To deploy ASReview service, see docs/LITERATURE_REVIEW.md
# Requirements:
# - ASReview service deployed on GPU host: pip install asreview[all]
# - ASREVIEW_SERVICE_URL configured in config.py
# Example: ASREVIEW_SERVICE_URL = "http://gpu-server:5275"

# Optional: Email Verification (OTP)
# Requires SMTP provider configuration
# Enable with: ENABLE_OTP_VALIDATION = True in config.py
# 
# For SendPulse REST API (recommended, 15k free emails/month):
#   pip install pysendpulse
#   export EMAIL_PROVIDER=sendpulse
#   export SENDPULSE_USER_ID="your-user-id"
#   export SENDPULSE_SECRET="your-secret"
#
# For SendPulse SMTP (12k free emails/month):
#   export EMAIL_PROVIDER=sendpulse_smtp
#   export SENDPULSE_SMTP_USERNAME="your-email"
#   export SENDPULSE_SMTP_PASSWORD="your-password"
#
# For Gmail SMTP:
#   export EMAIL_PROVIDER=gmail
#   export GMAIL_USERNAME="your@gmail.com"
#   export GMAIL_APP_PASSWORD="your-app-password"
#
# See docs/EMAIL_VERIFICATION_CONFIG_PLAN.md for full configuration options

# Development/Security Tools (Optional)
# Uncomment for development and security scanning
# pip-audit>=2.0.0  # Dependency vulnerability scanner
# bandit>=1.7.0  # Security linter for Python
# safety>=2.3.0  # Checks dependencies for known security vulnerabilities

